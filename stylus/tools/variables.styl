/*
 * Colors
 */

$color-blue = #0069b3
$color-date = #b0b1b3
$color-main = white
$color-header = black

$color-background = white

/*
 * Fonts
 */

$font-main = "Helvetica Neue", Helvetica, Arial, sans-serif

/*
 * Grid
 */

$grid-col-width = 50px
$grid-amound-of-cols = 12
$grid-col-margin = 15px

$grid-sizes = {}

for x in 1..$grid-amound-of-cols
	$grid-sizes[x] = ($grid-col-width * x + 2 * $grid-col-margin * (x - 1))
